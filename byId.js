//GET ELEMENT BY ID
// console.log(document.getElementById('new-task'));
let headerEl = document.getElementById('header');
// console.log(headerEl.textContent);
// console.log(headerEl.innerText);
// console.log(headerEl.innerHTML);
/**here is a difference in textContent and innerText which is innerText show which browser show/ browser output. innerHTML shows the HTML inside of another HTML element. */
// console.dir(headerEl);
// headerEl.style.color = 'red';
// headerEl.style.borderBottom = '1px solid purple';
// headerEl.style.fontSize = '4rem';
console.dir(document);
